\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{main}
\RequirePackage{titlesec,framed,fancyvrb,hyperref}

\usepackage{tikz}
\usepackage[most]{tcolorbox}

% box env {{{
\newtcolorbox{bbox}{
	%title={\centering \color{black}{\textsf{Example} }},
	boxrule=1pt,
	arc = 0.5mm,
	%sharp corners = downhill,
	enlarge top by=3mm,
	minipage boxed title*=-15cm,
	beamer,
	enhanced,
	center,
	%attach boxed title to top center={yshift=-2.5mm},
	attach boxed title to top left={yshift=-2mm, xshift=1cm},
	colback  = black!03!white,
	colframe = black!90!white,
	colbacktitle = white,
}
% }}}

%% link colors
\hypersetup{
    colorlinks=true,
    linkcolor={blue!55!black},
    citecolor={red!65!black},
    urlcolor={green!35!black},
}

%% custom section
\titleformat{\section}
    {\LARGE \bfseries}
    {\thesection}
    {.03\linewidth}
    {}%
    [{\titlerule[0pt]}]
%% custom subsection
\titleformat{\subsection}
    {\Large \bfseries}
    {\thesubsection}
    {.03\linewidth}
    {}%
    [{\titlerule[0pt]}]
%% custom subsubsection
\titleformat{\subsubsection}
    {\large \bfseries}
    {\thesubsubsection}
    {.03\linewidth}
    {}%
    [{\titlerule[0pt]}]

%% headings position
\titlespacing{\section}{0mm}{5mm}{5mm}
\titlespacing{\subsection}{0mm}{5mm}{5mm}
\titlespacing{\subsubsection}{0pt}{5mm}{5mm}

%% every h1 at a new page
%\let\oldsection\section
%\renewcommand\section{\newpage\oldsection}

%%% default fonts:
\setmainfont{Libertinus Serif}
\setmathfont{Libertinus Serif}
\setsansfont{Source Sans Pro}
%\setsansfont{Ubuntu}
\setmonofont{Fira Mono}
%\setmonofont{Ubuntu Mono}
%\setmonofont{Inconsolata}

%% add line number to code-blocks
%\DefineVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\},numbers=left}

%%% define shaded env
%%\ifcsname Shaded\endcsname
%\ifdefined\Shaded
%    \renewenvironment{Shaded}%
%        {\begin{quotation}\begin{snugshade}}
%        {\end{snugshade}\end{quotation}}
%\else
%    \newenvironment{Shaded}%
%        {\begin{quotation}\begin{snugshade}}
%        {\end{snugshade}\end{quotation}}
%\fi

%%\ifcsname foo\endcsname
%%  \message{\string\foo\space is defined}%
%%\else
%%  \message{no command \string\foo}%
%%\fi
%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%% quotation
%\renewenvironment{quote}%
%    {\begin{center}\begin{tabular}{|p{.9\textwidth}}\sffamily}%
%    {\end{tabular}\end{center}}
%\renewenvironment{quote}%
    %{\begin{quotation}\sffamily\noindent}%
    %{\end{quotation}}

%%% Simple verbatim
%\let\oldverbatim=\verbatim
%\let\oldendverbatim=\endverbatim
%\renewenvironment{verbatim}%
%    {\begin{quotation}\oldverbatim\noindent}%
%    {\oldendverbatim\end{quotation}}

%%% Paragraph indentation
%\setlength{\parindent}{15pt}
%\setlength\parindent{\defaultparindent}
\linespread{1.3}
